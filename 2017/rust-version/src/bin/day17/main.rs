fn count_next_ind(current: usize, len: usize, num: usize) -> usize {
    //current + num
}

fn day17(times: usize) {
    let mut stack = vec![0]; // yep, it is slow
    for i in 1..=times {}
}

fn main() {}
